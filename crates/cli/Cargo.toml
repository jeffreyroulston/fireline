[package]
name = "ga-fire-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "ga-fire"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
ga-fire-engine = { path = "../engine" }
serde_json = "1"
