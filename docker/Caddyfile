:80 {
	handle_path /api/* {
		reverse_proxy api:8080 {
			flush_interval -1
		}
	}

	# Keep the /play prefix — Next.js basePath expects it on every request.
	handle /play* {
		reverse_proxy play:3000
	}

	reverse_proxy web:3000
}
